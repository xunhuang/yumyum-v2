query BayArea {
  allVenues(
    first: 35
    condition: { metro: "bayarea", withOnlineReservation: "true" }
  ) {
    nodes {
      name
      stars
      city
      cuisine
      priceline
      withOnlineReservation
      coverImage
    }
  }
}
